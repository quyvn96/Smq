
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}
@section footerJS{
  <script src="~/Assets/client/js/controller/shoppingcart.js"></script>
}
<div class="single_top">
    <div class="single_grid">
        <div id="cartContent">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Product name</th>
                        <th>Image</th>
                        <th>Price</th>
                        <th>Quantity</th>
                        <th>Into money</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody id="cartBody"></tbody>
            </table>
            <button class="btn btn-success" id="btnContinue">Continue buying</button>
            <button class="btn btn-danger" id="btnDeleteAll">Delete cart</button>
            <button class="btn btn-default" id="btnCheckout">Payment</button>
            <div class="pull-right">
                Total money: <span id="lblTotalOrder"></span>
            </div>
        </div>
        <div id="divCheckout" style="display:none">
                <div class="reservation_top">
                    <div class="contact_right">
                        <h3>PAYMENT PRODUCT(s)</h3>
                        <div class="contact-form">
                            <form method="post" id="frmPayment">
                                @if (Request.IsAuthenticated)
                                {
                                    <label>
                                        <input type="checkbox" id="chkUserLoginInfo" />
                                        Use login information
                                    </label>
                                }      
                                <input type="text" class="textbox" name="name" id="txtName" placeholder="Full name">
                                <input type="text" class="textbox" name="address" id="txtAddress" placeholder="Address">
                                <input type="text" class="textbox" name="email" id="txtEmail" placeholder="Email">
                                <input type="text" class="textbox" name="phone" id="txtPhone" placeholder="Phone number">
                                <textarea id="txtMessage"></textarea>
                                <input type="button" id="btnCreateOrder" value="Payment" class="btn btn-info">
                                <div class="clearfix"> </div>
                            </form>
                        </div>
                    </div>
                </div>
        </div>
        <div class="clearfix"> </div>
    </div>
</div>
<script id="tplCart" type="x-tmpl-mustache">
    <tr>
        <td>{{ProductId}}</td>
        <td>{{ProductName}}</td>
        <td><img src="{{Image}}" height="50"/></td>
        <td>{{PriceF}}</td>
        <td><input type="number" data-id="{{ProductId}}" data-price="{{Price}}" value="{{Quantity}}" class="input txtQuantity" /></td>
        <td id="amount_{{ProductId}}">{{Amount}}</td>
        <td><button class="btn btn-danger btnDeleteItem" data-id="{{ProductId}}"><i class="fa fa-close"></i></button></td>
    </tr>
</script>
